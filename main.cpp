#include <QCoreApplication>
#include <iostream>
#include "Fileworker.h"
#include "game.h"
using namespace std;


int main(int argc, char *argv[])
{
    Game game;
    game.start();
    //commit master

    /*int amount;
    cout<<" enter amount tanks "<<endl;
    cin>>amount;
    Tank* pTanks = amount_tanks(amount);
    cout<<endl;
    deleteTanks(pTanks);

    Tank tank1;
    Tank tank2(-50,-25,"My_tank");
    tank1 = tank2;
    Tank tank3;
    tank1.show();
    Tank t3 = tank1;
    Tank t4(tank2);
    tank1.show();
    cout<<endl;
    tank2.show();
    tank1.shoot(tank2);
    cout<<endl;
    tank2.show();
    cout<<endl;
    tank1.live_tank();
    cout<<endl;
    t3.show();
    cout<<endl;
    t4.show();*/

    //1. зробити багато танків масив танків сам задає скільки танків//+++
    //2. згенерувати функцію яка генерує hp dmg name //+++
    //3. функція показує всю інфу???

    //3. метод або клас який буде писати характеристик в файл

    //1. звільнити пам'ять m_pWeapon
    //2. виділити нову память
    //3. копіювати память іншого танку використовуючи this
    //4. і вернути саму себе
    // REGISTER Github <-

    //1. Добавляємо межі
    //2. Реалізувати вихід чи паузу
    //3. переписати clrscreen (функція system) //++

    //1. перегрузити оператор += , -=
    //2. клас пуля добавити масив пуль в клас танк
    //new branch
    //git git
}
